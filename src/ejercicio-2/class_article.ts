class Article {
  constructor(public title: string, public autor: string[], public email: string[], 
    public keywords: string[], public abstrac: string, public date: string, public editorial: string,
    public appointments: number){

    }
   
  APAFormat() {
  
  }  
}