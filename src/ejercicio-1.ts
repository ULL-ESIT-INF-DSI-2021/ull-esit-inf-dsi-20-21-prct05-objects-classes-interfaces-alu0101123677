/**
 * Función que suma dos valores
 * @param valor1 tipo number con el primer valor
 * @param valor2 tipo number con el segundo valor
 * @returns retorna un valor suma de valor1 y valor2
 */
export function add(valor1: number, valor2:number): number {
 
  return valor1 + valor2;
}

console.log(add(5, 6));